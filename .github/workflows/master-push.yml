name: Symfony

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    
    - name: Install Dependencies
      run: echo ${{ secrets.USER }}
    - name: Install Dependencies
      run: echo ${{ env.USER }}
    
